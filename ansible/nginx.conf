server {
    listen 80 default_server;
    listen [::]:80 default_server;
    root /var/www/html;
    server_name fatfinger.click;

    location / {
        try_files $uri $uri/ /index.html =404;
    }

    location /imessageforwarder/ {
        proxy_pass http://localhost:3238;
    }
}
